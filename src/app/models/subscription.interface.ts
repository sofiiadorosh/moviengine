export interface Subscription {
  firstName: string,
  email: string,
  country: string,
  city: string,
  address: string,
  zip: string,
  genres: string[],
  agreement: boolean,
  lastName?: string,
}
